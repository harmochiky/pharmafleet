const initialData = {
  authenticated: true,
};

const coreReducer = (state = initialData, actions) => {
  switch (actions.type) {
    default:
      return state;
  }
};

export default coreReducer;
