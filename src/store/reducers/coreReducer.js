const initialData = {
  authenticated: null,
};

const coreReducer = (state = initialData, actions) => {
  switch (actions.type) {
    default:
      return state;
  }
};

export default coreReducer;
